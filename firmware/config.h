/**
 * Configuration header for T5AI Music Coach (C compatibility)
 * This file provides C compatibility for legacy code
 * For new code, use config.hpp instead
 */

#ifndef CONFIG_H
#define CONFIG_H

// Include C++ config for C compatibility
#include "config.hpp"

#endif // CONFIG_H
